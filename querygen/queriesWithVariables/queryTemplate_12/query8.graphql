query subqueryFilter1($producerID:ID, $vendorID:ID)
{
  Producer(nr:$producerID) {
    product {
      offers(where: { vendor: {nr:$vendorID} } )
      {
        price
        offerWebpage
        product {label comments}
      }
    }
  }
}
{
 	"producerID": 1,
	"vendorID": 8
}
 

query multipleFilters($producerID:ID,$date:Date, $commentOfVendorKeyword:String)
{
  Producer(nr:17) {
    product {
      offers(where:{
        vendor:{publishDate:{criterion:AFTER date:"2000-12-16"}} 
        AND: {
        vendor:{comment:{criterion:CONTAINS, pattern:"nubbins"}}
        }
      })
        {	
          price
          offerWebpage
          product {label comments}
        }
     }
  }
}

 

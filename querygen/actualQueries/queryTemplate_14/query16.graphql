query multipleFilters($producerID:ID,$date:Date, $commentOfVendorKeyword:String)
{
  Producer(nr:3) {
    product {
      offers(where:{
        vendor:{publishDate:{criterion:AFTER date:"2003-02-20"}} 
        AND: {
        vendor:{comment:{criterion:CONTAINS, pattern:"shifter"}}
        }
      })
        {	
          price
          offerWebpage
          product {label comments}
        }
     }
  }
}

 

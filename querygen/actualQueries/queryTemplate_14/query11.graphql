query multipleFilters($producerID:ID,$date:Date, $commentOfVendorKeyword:String)
{
  Producer(nr:22) {
    product {
      offers(where:{
        vendor:{publishDate:{criterion:AFTER date:"2005-11-11"}} 
        AND: {
        vendor:{comment:{criterion:CONTAINS, pattern:"connections"}}
        }
      })
        {	
          price
          offerWebpage
          product {label comments}
        }
     }
  }
}

 

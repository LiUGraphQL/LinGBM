query multipleFilters($producerID:ID,$date:Date, $commentOfVendorKeyword:String)
{
  Producer(nr:17) {
    product {
      offers(where:{
        vendor:{publishDate:{criterion:AFTER date:"2003-01-22"}} 
        AND: {
        vendor:{comment:{criterion:CONTAINS, pattern:"centralizers"}}
        }
      })
        {	
          price
          offerWebpage
          product {label comments}
        }
     }
  }
}

 

query multipleFilters($producerID:ID,$date:Date, $commentOfVendorKeyword:String)
{
  Producer(nr:16) {
    product {
      offers(where:{
        vendor:{publishDate:{criterion:AFTER date:"2003-10-06"}} 
        AND: {
        vendor:{comment:{criterion:CONTAINS, pattern:"beldam"}}
        }
      })
        {	
          price
          offerWebpage
          product {label comments}
        }
     }
  }
}

 

query multipleFilters($producerID:ID,$date:Date, $commentOfVendorKeyword:String)
{
  Producer(nr:11) {
    product {
      offers(where:{
        vendor:{publishDate:{criterion:AFTER date:"2004-11-03"}} 
        AND: {
        vendor:{comment:{criterion:CONTAINS, pattern:"heathy"}}
        }
      })
        {	
          price
          offerWebpage
          product {label comments}
        }
     }
  }
}

 

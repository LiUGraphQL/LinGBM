query multipleFilters($producerID:ID,$date:Date, $commentOfVendorKeyword:String)
{
  Producer(nr:16) {
    product {
      offers(where:{
        vendor:{publishDate:{criterion:AFTER date:"2001-09-12"}} 
        AND: {
        vendor:{comment:{criterion:CONTAINS, pattern:"realizations"}}
        }
      })
        {	
          price
          offerWebpage
          product {label comments}
        }
     }
  }
}

 

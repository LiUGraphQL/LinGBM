query multipleFilters($producerID:ID,$date:Date, $commentOfVendorKeyword:String)
{
  Producer(nr:2) {
    product {
      offers(where:{
        vendor:{publishDate:{criterion:AFTER date:"2002-01-04"}} 
        AND: {
        vendor:{comment:{criterion:CONTAINS, pattern:"querulously"}}
        }
      })
        {	
          price
          offerWebpage
          product {label comments}
        }
     }
  }
}

 

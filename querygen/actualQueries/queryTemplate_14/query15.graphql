query multipleFilters($producerID:ID,$date:Date, $commentOfVendorKeyword:String)
{
  Producer(nr:17) {
    product {
      offers(where:{
        vendor:{publishDate:{criterion:AFTER date:"2004-01-01"}} 
        AND: {
        vendor:{comment:{criterion:CONTAINS, pattern:"gagsters"}}
        }
      })
        {	
          price
          offerWebpage
          product {label comments}
        }
     }
  }
}

 

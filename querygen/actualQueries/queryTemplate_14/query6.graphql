query multipleFilters($producerID:ID,$date:Date, $commentOfVendorKeyword:String)
{
  Producer(nr:16) {
    product {
      offers(where:{
        vendor:{publishDate:{criterion:AFTER date:"2006-04-27"}} 
        AND: {
        vendor:{comment:{criterion:CONTAINS, pattern:"catwalk"}}
        }
      })
        {	
          price
          offerWebpage
          product {label comments}
        }
     }
  }
}

 

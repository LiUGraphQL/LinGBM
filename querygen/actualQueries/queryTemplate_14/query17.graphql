query multipleFilters($producerID:ID,$date:Date, $commentOfVendorKeyword:String)
{
  Producer(nr:4) {
    product {
      offers(where:{
        vendor:{publishDate:{criterion:AFTER date:"2001-07-28"}} 
        AND: {
        vendor:{comment:{criterion:CONTAINS, pattern:"phew"}}
        }
      })
        {	
          price
          offerWebpage
          product {label comments}
        }
     }
  }
}

 

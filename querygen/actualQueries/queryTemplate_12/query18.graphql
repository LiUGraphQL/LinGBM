query subqueryFilter1($producerID:ID, $vendorID:ID)
{
  Producer(nr:9) {
    product {
      offers(where: { vendor: {nr:7} } )
      {
        price
        offerWebpage
        product {label comments}
      }
    }
  }
}

 

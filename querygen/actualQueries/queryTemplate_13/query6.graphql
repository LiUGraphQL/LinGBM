query subqueryFilter2($producerID:ID, $date: Date)
{
  Producer(nr:1) {
    product {
      offers(where:{ vendor:{ publishDate:{criterion:AFTER date:"2002-06-29"}}})
      {	
        price
        offerWebpage
        product {label comments}
      }
    }
  }
}

 

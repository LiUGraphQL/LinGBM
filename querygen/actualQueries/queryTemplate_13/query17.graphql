query subqueryFilter2($producerID:ID, $date: Date)
{
  Producer(nr:2) {
    product {
      offers(where:{ vendor:{ publishDate:{criterion:AFTER date:"2000-10-02"}}})
      {	
        price
        offerWebpage
        product {label comments}
      }
    }
  }
}

 

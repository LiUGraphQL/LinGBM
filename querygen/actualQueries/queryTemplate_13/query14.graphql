query subqueryFilter2($producerID:ID, $date: Date)
{
  Producer(nr:3) {
    product {
      offers(where:{ vendor:{ publishDate:{criterion:AFTER date:"2005-08-24"}}})
      {	
        price
        offerWebpage
        product {label comments}
      }
    }
  }
}

 

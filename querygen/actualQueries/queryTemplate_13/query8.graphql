query subqueryFilter2($producerID:ID, $date: Date)
{
  Producer(nr:9) {
    product {
      offers(where:{ vendor:{ publishDate:{criterion:AFTER date:"2003-11-19"}}})
      {	
        price
        offerWebpage
        product {label comments}
      }
    }
  }
}

 

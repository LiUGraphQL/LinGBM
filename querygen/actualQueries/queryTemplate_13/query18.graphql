query subqueryFilter2($producerID:ID, $date: Date)
{
  Producer(nr:9) {
    product {
      offers(where:{ vendor:{ publishDate:{criterion:AFTER date:"2005-07-30"}}})
      {	
        price
        offerWebpage
        product {label comments}
      }
    }
  }
}

 

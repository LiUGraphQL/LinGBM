query subqueryFilter2($producerID:ID, $date: Date)
{
  Producer(nr:8) {
    product {
      offers(where:{ vendor:{ publishDate:{criterion:AFTER date:"2003-01-01"}}})
      {	
        price
        offerWebpage
        product {label comments}
      }
    }
  }
}

 

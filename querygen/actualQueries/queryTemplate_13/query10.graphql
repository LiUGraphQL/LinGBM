query subqueryFilter2($producerID:ID, $date: Date)
{
  Producer(nr:5) {
    product {
      offers(where:{ vendor:{ publishDate:{criterion:AFTER date:"2006-08-27"}}})
      {	
        price
        offerWebpage
        product {label comments}
      }
    }
  }
}

 

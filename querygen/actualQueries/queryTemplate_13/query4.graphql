query subqueryFilter2
{
  Producer(nr:2) {
    product {
      offers(where:{ vendor:{ publishDate:{criterion:AFTER date:"2001-08-05"}}})
      {	
        price
        offerWebpage
        product {label comments}
      }
    }
  }
}

 

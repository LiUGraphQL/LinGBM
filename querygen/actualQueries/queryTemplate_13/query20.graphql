query subqueryFilter2($producerID:ID, $date: Date)
{
  Producer(nr:4) {
    product {
      offers(where:{ vendor:{ publishDate:{criterion:AFTER date:"2006-06-05"}}})
      {	
        price
        offerWebpage
        product {label comments}
      }
    }
  }
}

 
